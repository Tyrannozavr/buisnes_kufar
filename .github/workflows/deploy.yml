name: Deploy to production

on:
  push:
    branches: [ "master" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Настройка SSH
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts
          echo "Host target-server" >> ~/.ssh/config
          echo "  HostName ${{ secrets.SERVER_IP }}" >> ~/.ssh/config
          echo "  User root" >> ~/.ssh/config
          echo "  IdentityFile ~/.ssh/id_ed25519" >> ~/.ssh/config
          echo "  StrictHostKeyChecking no" >> ~/.ssh/config

      - name: Deploy with validation
        run: |
          ssh target-server "
            set -e  # Прерывать при любой ошибке
            
            cd buisnes_kufar
            
            # 1. Проверяем изменения
            git fetch origin
            if git diff --quiet HEAD origin/master; then
              echo '✅ Нет новых изменений. Выходим.'
              exit 0
            fi
            
            # 2. Создаем точку отката
            current_commit=\$(git rev-parse HEAD)
            
            # 3. Применяем изменения
            git reset --hard origin/master
            
            # 4. Пытаемся собрать и запустить контейнеры
            if docker compose up -d --build --force-recreate; then
              # 5. Даем время на запуск
              sleep 15
              
              # 6. Проверяем ОСНОВНЫЕ контейнеры
              REQUIRED_CONTAINERS='backend frontend nginx'
              running_containers=\$(docker compose ps --services --filter 'status=running')
              
              for container in \$REQUIRED_CONTAINERS; do
                if ! echo \"\$running_containers\" | grep -q \"^\$container\$\"; then
                  echo \"❌ Контейнер \$container не запущен!\"
                  echo \"=== Логи контейнера ===\"
                  docker compose logs \$container
                  exit 1
                fi
              done
              
              echo '✅ Все контейнеры успешно запущены!'
            else
              echo '❌ Ошибка сборки/запуска контейнеров! Откатываем...'
              git reset --hard \$current_commit
              docker compose down
              docker compose up -d
              exit 1
            fi
          " || exit 1  # Важно: завершаем step с ошибкой если ssh команда завершилась с ошибкой

      - name: Clean up old images
        if: success()
        run: |
          ssh target-server "
            echo '🧹 Очистка неиспользуемых Docker образов...'
            docker image prune -af
            echo '✅ Очистка завершена.'
          "