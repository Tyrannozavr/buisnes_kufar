# Этап 1 — 10 000 ₽. Доработки реализации Сергея

**Статус:** ✅ реализован (`feature/etap1-schet-dorabotki-sergeya` → `preprod`) — §1.1–1.7  
**Зависимости:** нет (первый этап)  
**Следующий:** [etap-02-schet-na-oplatu.md](etap-02-schet-na-oplatu.md)

**Цель:** привести уже влитый код Сергея к рабочему состоянию по **базовому ТЗ** и устранить баги перед расширенным ТЗ. Новый функционал (счёт-договор, оферта, диалоги с договорами) — **не входит**.

> Сергей уже влил в `preprod` ядро: `useDeals`, `Bill/`, `BillMenu`, DOC/PDF. Этап — точечные исправления, не переписывание. См. [git-status.md](git-status.md).

---

## 1.1. ЛК «Данные компании» (§2 ТЗ) ✅

| Поле / поведение | Было | Сделано |
|------------------|------|---------|
| БИК, корр. счёт, банк, расч. счёт | `CompanyPaymentsSection.vue` — есть | `useCompanyBillRequisites` + `fillBillData` подтягивает из ЛК → `deal.seller` |
| Ставка НДС 5–25, **20%** | 20% не было | `VAT_RATE_OPTIONS` в ЛК и `BillMenu` |
| Индекс | В типах есть | Поле в UI «Информация о компании» + API + `fillBillData` |
| Должностные лица | `PersonSelector` в счёте | При `fillBillData` (role=seller): officials из ЛК, если в счёте пусто |

**Задачи:**

1. ✅ Composable **`useCompanyBillRequisites(companyId)`** — `frontend/composables/useCompanyBillRequisites.ts`
2. ✅ **`normalizePrice`** — всегда 2 знака (`1,200.00`); пропись — `total_amount_word` с API, fallback на клиент

---

## 1.2. Баг: верхняя таблица реквизитов (`Bill/Bill.vue`) ✅

**Было:** банковский блок на **`billData.buyer`**.  
**Сделано:** все реквизиты банка и «Получатель» — **`seller`**.

| Поле | Источник |
|------|----------|
| Банк получателя | `seller.bankName` |
| БИК | `seller.bic` |
| Сч. № верхний | `seller.correspondentBankAccount` |
| ИНН, КПП | `seller.inn`, `seller.kpp` |
| Сч. № нижний | `seller.accountNumber` |
| Получатель | ОПФ + `seller.companyName` |

---

## 1.3. «Заполнить данными» (`InsertButtons.vue`) ✅

**Было:** переход на **последнюю** закупку/продажу.  
**Сделано:** подстановка в **текущую** сделку + модалка «Данные будут изменены. Продолжить?»

1. ✅ По вкладке: заказ — позиции сделки; счёт — `fillBillData()` для текущего `dealId`
2. ✅ Новый счёт после `createBill`: только **номер + дата**; полное заполнение — по кнопке
3. ✅ Убраны «последняя закупка/продажа»

---

## 1.4. `BillMenu.vue` — дефолты ✅

| Элемент | Действие |
|---------|----------|
| Доп. информация | ✅ **Вкл. по умолчанию** + текст `ADDITIONAL_INFO_BILL.PAYMENT` |
| Ставка НДС | ✅ Из ЛК, если не «Без НДС» |
| Опечатка «инфорамация» | ✅ Исправить |

---

## 1.5. «Создать счет» (минимум) ✅

- После `createBill` — не перегружать форму (см. 1.3).
- Smoke-test маски `00012 от …` на бэкенде.

---

## 1.6. Роли ✅

- Покупатель: read-only на `#bill`.
- Фото/сканы — **этапы 2–4** (внутри 70k), см. [payment-and-schedule.md](payment-and-schedule.md).

---

## 1.7. Мелкие баги ✅

| Проблема | Файл | Исправление |
|----------|------|-------------|
| «ДОГОВОР на основании» → вкладка 3 | `OrderMenu.vue` | ✅ `activeTab = '5'`, `#contract` |
| НДС без галки виден | `Bill.vue` | ✅ Строка «В том числе НДС» только при `vatRateCheck` |
| `console.log` | `BillMenu.vue`, `CompanyPaymentsSection.vue` | ✅ Удалены ранее |

---

## Приёмка (10 000 ₽)

- [x] Composable реквизитов; 20% НДС; индекс (§1.1)
- [x] Верхняя таблица → **seller** (§1.2)
- [x] Новый счёт: номер/дата; «Заполнить данными» → текущая сделка (§1.3)
- [x] Дефолты галок по ТЗ (§1.4)
- [x] createBill / маска номера (§1.5)
- [x] Read-only покупатель на `#bill` (§1.6)
- [x] `OrderMenu` → вкладка 5; НДС по галке (§1.7)

**Срок:** 2–3 дня.
