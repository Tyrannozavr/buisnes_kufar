# Этап 2 — 20 000 ₽. Фундамент документооборота + «Счет на оплату»

**Статус:** 🔄 в работе — §2.2 ✅ · §2.3 ✅  
**Зависимости:** [etap-01-dorabotki-sergeya.md](etap-01-dorabotki-sergeya.md) принят  
**Следующий:** [etap-03-sozdanie-scheta-dogovor-postavki.md](etap-03-sozdanie-scheta-dogovor-postavki.md)

**Цель:** закрыть **базовый контур сделки** (ОКЕИ, checkout, таблицы «Товары», закладка «Заказ») и довести **«Счет на оплату»** до полного §5 ТЗ + фото/сканы на «Заказ» и «Счет».

> Объём этапа ~⅓ от 70k (20k). Всё из [tz-requirements.md](tz-requirements.md), помеченное **→ этап 2**, входит сюда.

---

## Scope

### 2.1. ОКЕИ (§A.1)

| Задача | Детали |
|--------|--------|
| Справочник | Коды ОКЕИ у единиц измерения в БД/API |
| UI | Автоподстановка кода в **заказе** и **счёте** по выбранной ед. изм. |

### 2.2. Страница подтверждения (§A.2) ✅

| Задача | Детали | Статус |
|--------|--------|--------|
| Checkout | Заказы по продавцам; товары и услуги — отдельные заказы | ✅ |
| После «Подтвердить» | Запись в «Закупки» / «Продажи» | ✅ |
| Уведомление | Сообщение продавцу в чате о новом заказе | ✅ |

#### Что сделано (по ТЗ §A.2)

**Фронт — `frontend/pages/checkout.vue`**

- Группировка корзины по **`{продавец} + {Товары|Услуги}`** (ключ `companyId-orderType`).
- Заголовки по ТЗ: «Заказ на поставку **товаров**…» / «Заказ на оказание **услуг**…».
- Кнопка **«Подтвердить»** на каждый блок (раньше было «Отправить заказ» без разделения типов).
- Подтверждение **по одной группе** — товар и услуга одного продавца дают **два** заказа.
- После успеха — toast с переходом в **Закупки**; позиции группы убираются из корзины.
- Состояние загрузки `submittingGroupKey`, чтобы не дублировать отправку.

**API — `POST /api/v1/purchases/checkout`**

- Ответ **`CheckoutResponse`**: `{ deals: DealResponse[] }` (не один deal).
- **`create_deals_from_checkout`**: группировка по `(seller_id, OrderType)` — товары и услуги **разные** сделки.
- Тип заказа: `productType` из корзины или тип позиции в каталоге.
- Продавец: `companyId` в теле **или** определение по **slug** товара в каталоге (без `companyId` в payload).
- В `DealResponse` добавлено поле **`deal_type`** (`Товары` / `Услуги`).

**Чат — `backend/app/api/purchases/services/checkout_chat_notify.py`**

- После создания заказа — сообщение продавцу: номер заказа, тип (товары/услуги), список позиций.
- Ссылка **«Просмотр заказа»** → `/profile/editor?dealId=…&role=seller#order`.
- Чат создаётся/находится между компаниями автоматически.

**Дополнительно (не было явно в §2.2, но по переписке / для приёмки)**

- Тесты: `backend/tests/test_checkout_api.py` — split товар/услуга, один тип, уведомление в чат, seller по slug.
- Seed для ручной проверки: `backend/scripts/ensure_schet_test_users.py` — товар `test-checkout-bolt-m8` и услуга `test-checkout-montazh`.
- Инструкция проверки: [test-users.md](test-users.md) §2.2.
- `frontend/api/purchases.ts` — в checkout уходит `productType`; `companyId` продавца на бэкенде, не из buyer в теле.

**Файлы**

| Назначение | Путь |
|------------|------|
| Страница checkout | `frontend/pages/checkout.vue` |
| Запрос checkout | `frontend/queries/purchases.ts`, `frontend/api/purchases.ts` |
| Сервис | `backend/app/api/purchases/services/__init__.py` → `create_deals_from_checkout` |
| Уведомление в чат | `backend/app/api/purchases/services/checkout_chat_notify.py` |
| Роут | `backend/app/api/purchases/router.py` |
| Схемы | `CheckoutResponse`, `deal_type` в `schemas/__init__.py` |
| Тесты | `backend/tests/test_checkout_api.py` |

### 2.3. Таблицы «Товары» — Продажи / Закупки (§A.3) ✅

| Задача | Детали | Статус |
|--------|--------|--------|
| Колонки | Заказ, Дата, контрагент, Состояние, Счет, Договор поставки, Сопровод., Счёт-фактура, Другие | ✅ |
| Нумерация | Маска **00000** + дата + ` г.` в ссылках документов | ✅ |
| Сортировки | Заказ, Дата, контрагент (А–Я) | ✅ |
| Действия | «Создать счет» / «Создать документ» / «Создать счет-фактуру» (продавец); «Просмотр» (покупатель) | ✅ |
| Фильтр | Только сделки `dealType === 'Товары'`; вкладка «Услуги» — заглушка до этапа 4 | ✅ |
| Покупатель | `role=buyer` → read-only в редакторе | ✅ |

#### Что сделано

**`GoodsColumns.vue`**

- Фильтр **только товарные** сделки на закладке «Товары».
- Все **9 колонок** по ТЗ; «Сопроводительные документы» (было «Закрывающие»).
- Колонка **«Счет-фактура»** → `#invoice` (бланк — заглушка, навигация работает).
- Формат ссылок: `formatDocumentLinkLabel` — `00012 от 08.04.2025 г.`
- **Сортировка** через `v-model:sorting` на `UTable` (Заказ, Дата, контрагент).
- Исправлены hash: `#accompanyingDocuments`, `#othersDocument` (было `#closingDocuments`).
- **«Создать счет»**: `createBill` + `markBillAwaitingFill` (пустой бланк §1.3), без `fillFromDeal`.

**`purchases.vue` / `sales.vue`**

- Вкладки **Товары | Услуги** (услуги — placeholder этап 4).

**`editor/index.vue`**

- Покупатель (`role=buyer`) — **read-only**; поставщик редактирует.
- Вкладки «Сопроводительные» и «Счет-фактура» доступны для перехода из таблицы.

**Файлы:** `GoodsColumns.vue`, `purchases.vue`, `sales.vue`, `types/purchases.ts`, `utils/normalize.ts`, `editor/index.vue`


### 2.4. Закладка «Заказ» — форма и команды (§B.1)

| Задача | Детали |
|--------|--------|
| Форма A4 | Организация, адрес, телефон, номер, дата, таблица, итоги, пропись |
| Должностные лица | Блок на форме заказа из ЛК |
| Команды | Предпросмотр, Печать, DOC, PDF, **Удалить данные**, Сохранить |
| Заполнить данными | На базе исправлений этапа 1 |

### 2.5. «Счет на оплату» — полное §5 (§E)

| Блок | Задачи |
|------|--------|
| Бланк | Вёрстка по приложению; жирные линии; «Основание» по галке |
| Итоги и НДС | Формула; скрытие/пустое поле без галки |
| Срок оплаты | *«Счет действителен в течении … рабочих дней…»* |
| Доп. информация | Текст §5.6 (дефолты — этап 1) |
| Подписи | 1–3 из ЛК при «Заполнить данными» |
| Сохранение | Все поля в API; reload |
| Общие команды счёта | Предпросмотр, печать, DOC, PDF, **Удалить данные**, Сохранить |
| DOCX/PDF | Шаблон = форма на экране |
| Формат чисел | `1,200.00`; пропись с копейками |

### 2.6. Фото/Сканы §4.3 — компонент + первые вкладки (§B.3)

| Задача | Детали |
|--------|--------|
| UI-блок | «Выберите файл», превью, Удалить, Сохранить локально |
| Вкладки | **Заказ**, **Счет на оплату** |
| Отправить контрагенту | Файл в чат + блок сканов у заявки |
| Покупатель | Read-only просмотр (референс — Elba в ТЗ) |
| API | Привязка к `deal_id` + тип вкладки; хранение файла |

---

## Приёмка (20 000 ₽)

- [ ] ОКЕИ: код подставляется в заказе и счёте
- [x] Checkout: подтверждение → Закупки/Продажи + уведомление в чат (§2.2)
- [x] Таблицы «Товары»: колонки, сортировки, «Создать счет» / «Просмотр» (§2.3)
- [ ] Заказ: форма A4, должностные лица, все команды (вкл. «Удалить данные»)
- [ ] Счет на оплату: полный цикл + все галки по §5
- [ ] DOC/PDF/просмотр = экран; покупатель read-only
- [ ] Фото/сканы работают на «Заказ» и «Счет на оплату»

**Срок:** 5–7 дней.
