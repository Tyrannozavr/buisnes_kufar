# Ручная приёмка — этап 7 (ТЗ_15)

Аккаунты: [test-users.md](test-users.md). Пароль: `123456`.

## §7.1 Вкладки

- [ ] `/profile` — вкладки «Данные компании» и «Данные заполнения»
- [ ] Seller и buyer: обе вкладки открываются

## §7.2 Адреса

- [ ] CRUD адресов погрузки и приёма
- [ ] Один адрес → автоматически «По умолчанию»
- [ ] Смена default снимает флаг с других того же kind
- [ ] Удаление default переназначает оставшемуся

## §7.3 Товар

- [ ] Нет радио Товар/Услуга
- [ ] Вес нетто / брутто сохраняются
- [ ] Вкладка Продажи → Услуги (сделка 4) по-прежнему есть

## §7.4 Заполнить данными

- [ ] Seller в редакторе: кнопка активна при сделке, без требования «все поля заполнены»
- [ ] Buyer: кнопка недоступна
- [ ] После fill бланк содержит доступные данные, пустые остаются пустыми

## API smoke

```bash
# после login получить token
curl -s -X POST http://localhost:8000/api/v1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"seller@gmail.com","password":"123456"}'

curl -s http://localhost:8000/api/v1/company/me/fill-addresses \
  -H "Authorization: Bearer $TOKEN"
```
