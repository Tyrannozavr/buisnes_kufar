# Ручная приёмка — этап 7 (ТЗ_15)

Аккаунты: [test-users.md](test-users.md). Пароль: `123456`.  
**UI:** http://localhost:3014

## §7.1 Вкладки

- [x] `/profile` — вкладки «Данные компании» и «Данные заполнения»
- [x] Seller и buyer: обе вкладки открываются

## §7.2 Адреса

- [x] CRUD адресов погрузки и приёма
- [x] Один адрес → автоматически «По умолчанию»
- [x] Смена default снимает флаг с других того же kind
- [x] Удаление default переназначает оставшемуся

## §7.3 Товар

- [x] Нет радио Товар/Услуга
- [x] Вес нетто / брутто сохраняются
- [x] Вкладка Продажи → Услуги (сделка 4) по-прежнему есть

## §7.4 Заполнить данными

- [x] Seller в редакторе: кнопка активна при сделке, без требования «все поля заполнены»
- [x] Buyer: кнопка недоступна
- [x] После fill бланк содержит доступные данные, пустые остаются пустыми

## API smoke

```bash
curl -s -X POST http://localhost:8014/api/v1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"login":"seller@gmail.com","password":"123456"}'

curl -s http://localhost:8014/api/v1/company/me/fill-addresses \
  -H "Authorization: Bearer $TOKEN"
```
