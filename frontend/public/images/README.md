# Изображения для шапки сайта

Положите сюда файлы для отображения в хедере:

- `logo.jpg` — логотип
- `companyNameWhite.png` — название компании

Если файлов нет, в шапке будут битые картинки (404). В хедере используется обычный `<img>`, не NuxtImg, чтобы не было 500 от `_ipx`.
